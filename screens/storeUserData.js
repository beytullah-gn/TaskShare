/*import AsyncStorage from '@react-native-async-storage/async-storage';

const storeUserData = async () => {
  const users = [
    { id: '1', username: 'admin', password: '1234', accountType: 'Admin' },
    { id: '2', username: 'worker1', password: '12345', accountType: 'worker' },
    { id: '3', username: 'worker2', password: '123456', accountType: 'worker' },
    { id: '4', username: 'worker3', password: '1234567', accountType: 'worker' },
  ];
  
  try {
    const jsonValue = JSON.stringify(users);
    await AsyncStorage.setItem('@users', jsonValue);
    console.log("Kullanıcı verileri başarıyla kaydedildi.");
  } catch (e) {
    console.error("Kullanıcı verileri kaydedilemedi: ", e);
  }
};

export default storeUserData;*/

